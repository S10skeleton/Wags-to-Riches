<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Profile</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/jass.css">
<link rel="stylesheet" href="/css/style.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>

<!-- Saved Pets Section -->
<div class="container mt-4">
{{!-- 
    <div class="mb-4">
        <p>{{name}}</p>
    </div> --}}

    <h1 class="mb-4 text-center">Your Saved Pets</h1>

    <div class="row">
        <!-- Pet 1 -->
        <div class="col-md-4 mb-4">
            <div class="card">
                <img src="{{image}}" class="card-img-top" alt="Pet 1">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <p class="card-text"><strong>Breed:</strong> {{breed}}</p>
                    <p class="card-text"><strong>Location:</strong> {{location}}</p>
                </div>
            </div>
        </div>

        <!-- Pet 2 -->
        <div class="col-md-4 mb-4">
            <div class="card">
                <img src="{{image}}" class="card-img-top" alt="Pet 2">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <p class="card-text"><strong>Breed:</strong> {{breed}}</p>
                    <p class="card-text"><strong>Location:</strong>{{location}}</p>
                </div>
            </div>
        </div>

       <!-- Pet 3 -->
        <div class="col-md-4 mb-4">
            <div class="card">
                <img src="{{image}}" class="card-img-top" alt="Pet 2">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <p class="card-text"><strong>Breed:</strong> {{breed}}</p>
                    <p class="card-text"><strong>Location:</strong>{{location}}</p>
                </div>
            </div>
        </div>
        <!-- Add more cards for additional saved pets as needed -->
    </div>
</div>

<!-- Bootstrap JS -->
<script src="./js/profile.js"></script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

</body>
</html>

